/**
 * Import ReactDOM from a single point and reexport it to the whole project.
 * Using this setup changing the ReactDOM version or location is as simple as changing a single url.
 */
export ReactDOM from "https://cdn.skypack.dev/react-dom";
